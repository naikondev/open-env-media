# Sample EMDP package (repeat photography)

Three annual visits to the same forest stake (`locationID` reused, `fixedPoint=true`). Frames are aligned year-to-year. Canopy cover is a measurement. "Canopy loss" is an assertion on the latest visit. ESA WorldCover is stored as a context join.
